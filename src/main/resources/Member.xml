<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="memberns">
	<resultMap type="member" id="memberResult">
		<id property="userid" column="userid" />
		<result property="username" column="username" />
		<result property="password" column="password" />
		<result property="email" column="email" />
		<result property="status" column="status" />
		<result property="created" column="created" />
		<result property="modified" column="modified" />
		<result property="profile_image" column="profile_image" />
		<result property="tel" column="tel" />
		<result property="birth" column="birth" />
	</resultMap>

	<select id="select" parameterType="String" resultType="member">
		select * from member where userid=#{member_id}
	</select>

	<select id="search" parameterType="String" resultMap="memberResult">
		select * from member where userid like '%'|| #{searchTerm} || '%'
	</select>
	
	<insert id="insert" parameterType="member">
		insert into MEMBER values (#{userid}, #{username}, #{password}, #{email}, 1, sysdate, sysdate, '/img_all/default_profile.png', null, null)
	</insert>
	
	<update id="update" parameterType="member">
		update member set username = #{username}, password= #{password}, email=
		#{email}, modified = sysdate,
		profile_image = #{profile_image}, tel= #{tel} where userid = #{userid}
	</update>
	
	
</mapper>
