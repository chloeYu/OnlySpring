<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="friendlistns">
	<resultMap type="friendlist" id="friendlistResult">
		<id property="uid1" column="uid1" />
		<id property="uid2" column="uid2" />
		<result property="status" column="status" />
	</resultMap>
	
	<resultMap type="member" id="memberResult">
		<id property="userid" column="userid" />
		<result property="username" column="username" />
		<result property="password" column="password" />
		<result property="email" column="email" />
		<result property="tel" column="tel" />
		<result property="birth" column="birth" />
		<result property="profile_image" column="profile_image" />
		<result property="status" column="status" />
		<result property="created" column="created" />
		<result property="modified" column="modified" />
	</resultMap>
	<select id="friendList" parameterType="String" resultMap="memberResult">
		select a.* from (select m.* from member m join follow f on (f.uid1 = m.userid or f.uid2 = m.userid) where (f.status = 1 and (f.uid1=#{member_id} or f.uid2=#{member_id} ))) a where a.userid != #{member_id}
	</select>
	
	<select id="pendingList" parameterType="String" resultMap="memberResult">
		select member.* from member join follow on (uid2 = userid) where follow.status = 2 and uid1=#{member_id}
	</select>
	
	<select id="requestList" parameterType="String" resultMap="memberResult">
		select member.* from member join follow on (uid1 = userid )where follow.status = 2 and uid2=#{member_id}
	</select>
	
	<update id="update" parameterType="friendlist">
		update follow set status=#{status} where uid1=#{uid1} and uid2=#{uid2}
	</update>
</mapper>